# 🎯 MainWindow 統合完了ガイド

## 概要

`app/gui/main_window.py` に NavigationPanel と新規プロジェクト作成機能を完全統合しました。

---

## 📦 統合された機能

### 1. **NavigationPanel 統合**
- ✅ `NavigationPanel` クラスを使用
- ✅ コールバックベースの操作
- ✅ プログレスバー機能

### 2. **新規プロジェクト作成**
- ✅ `ProjectDialog` ダイアログ
- ✅ `start_analysis()` メソッド
- ✅ バックグラウンド処理（threading）
- ✅ ローディング画面
- ✅ エラーハンドリング

### 3. **ビュー管理**
- ✅ ウェルカム画面
- ✅ MacroView（全体マップ）
- ✅ MicroView（詳細比較）
- ✅ ローディング画面

### 4. **ContentAnalyzer 統合**
- ✅ OCREngine 初期化
- ✅ ContentAnalyzer 初期化
- ✅ 自動マッチング

---

## 🎨 UI構成

### ウィンドウレイアウト

```
┌────────────────────────────────────────────────────┐
│ OCR 比較ツール - プロジェクトマネージャー           │
├──────────────┬─────────────────────────────────────┤
│              │                                     │
│ Navigation   │        Content Area                 │
│ Panel        │                                     │
│              │  ┌───────────────────────────┐     │
│ ➕ 新規      │  │                           │     │
│   プロジェクト│  │   ウェルカム画面          │     │
│              │  │   または                  │     │
│ ──────────   │  │   MacroView/MicroView     │     │
│              │  │   または                  │     │
│ 🗺️ ビュー    │  │   ローディング画面        │     │
│ 🗺️ 全体マップ│  │                           │     │
│              │  └───────────────────────────┘     │
│ ──────────   │                                     │
│              │                                     │
│ 📂 読込      │                                     │
│ 🌐 Web       │                                     │
│ 📁 PDF       │                                     │
│              │                                     │
│ (以下省略)   │                                     │
└──────────────┴─────────────────────────────────────┘
```

---

## 🚀 使用フロー

### 1. アプリ起動

```python
from app.gui.main_window import MainWindow

app = MainWindow()
app.mainloop()
```

または

```bash
python app/gui/main_window.py
python test_main_window.py  # テスト用
```

### 2. 新規プロジェクト作成

```
ウェルカム画面
  ↓
「➕ 新規プロジェクト」クリック
  ↓
ProjectDialog 表示
  ↓
設定入力（URL, PDF, OCR, 閾値）
  ↓
「🚀 分析開始」クリック
  ↓
ローディング画面
  ├─ 🌐 Webクロール中...
  ├─ 📁 PDF読込中...
  ├─ 🔍 OCR実行中...
  └─ ⚡ マッチング中...
  ↓
完了ダイアログ
  ↓
MacroView（全体マップ）表示
```

---

## 🔧 コールバック一覧

NavigationPanel に設定されているコールバック：

| ボタン | コールバック | 説明 |
|--------|-------------|------|
| ➕ 新規プロジェクト | `open_new_project_dialog()` | ダイアログを開く |
| 🗺️ 全体マップ | `show_macro_view()` | MacroView表示 |
| 🌐 Web一括クロール | `crawl_web()` | 新規プロジェクトへ誘導 |
| 📁 PDF一括読込 | `load_pdfs()` | 新規プロジェクトへ誘導 |
| ⚡ 一括マッチング | `match_all()` | マッチング実行 |
| 🔍 OCR実行 | `run_ocr()` | 実装予定 |
| 📤 Excel出力 | `export_excel()` | 実装予定 |
| 💾 プロジェクト保存 | `save_project()` | 実装予定 |
| 📂 プロジェクト読込 | `load_project()` | 実装予定 |

---

## 📊 クラス構造

```python
MainWindow(ctk.CTk)
├── __init__()
│   ├── _setup_ui()
│   │   ├── NavigationPanel 作成
│   │   └── content_area 作成
│   └── _initialize_engines()
│       ├── OCREngine 初期化
│       └── ContentAnalyzer 初期化
│
├── ビュー管理
│   ├── _show_welcome()           # ウェルカム画面
│   ├── show_macro_view()         # 全体マップ
│   ├── show_micro_view()         # 詳細比較
│   └── _open_micro_view(pair)    # ペア付き詳細比較
│
├── 新規プロジェクト機能
│   ├── open_new_project_dialog() # ダイアログ起動
│   ├── start_analysis(config)    # 分析開始
│   ├── _show_loading_screen()    # ローディング表示
│   ├── _update_loading_message() # メッセージ更新
│   ├── _crawl_web_pages()        # Webクロール
│   ├── _load_pdf_pages()         # PDF読込
│   ├── _on_analysis_complete()   # 完了処理
│   └── _on_analysis_error()      # エラー処理
│
└── その他の機能
    ├── crawl_web()
    ├── load_pdfs()
    ├── match_all()
    ├── run_ocr()
    ├── export_excel()
    ├── save_project()
    └── load_project()
```

---

## 🎯 主要メソッド

### `open_new_project_dialog()`

```python
def open_new_project_dialog(self):
    """新規プロジェクトダイアログを開く"""
    dialog = ProjectDialog(
        self,
        on_start=self.start_analysis
    )
    self.wait_window(dialog)
```

### `start_analysis(config)`

```python
def start_analysis(self, config: Dict):
    """分析を開始（バックグラウンド処理）"""
    self._show_loading_screen()
    self.nav_panel.show_progress()
    
    def _run_analysis():
        try:
            # 1. Webクロール
            web_results = self._crawl_web_pages(...)
            
            # 2. PDF読込
            pdf_results = self._load_pdf_pages(...)
            
            # 3. マッチング
            pairs = self.analyzer.compute_auto_matches(...)
            
            # 4. 完了
            self.after(0, lambda: self._on_analysis_complete(...))
        except Exception as e:
            self.after(0, lambda: self._on_analysis_error(e))
        finally:
            self.after(0, self.nav_panel.hide_progress)
    
    threading.Thread(target=_run_analysis, daemon=True).start()
```

---

## 🔄 データフロー

```
ProjectDialog
  ↓ config = {url, depth, max_pages, pdf_file, ...}
MainWindow.start_analysis(config)
  ↓ threading.Thread
  ├─ _crawl_web_pages()
  │   └─ EnhancedScraper.crawl_site()
  │
  ├─ _load_pdf_pages()
  │   └─ PDFLoader.load_pdf()
  │
  └─ ContentAnalyzer.compute_auto_matches()
  ↓
_on_analysis_complete()
  └─ show_macro_view()
      └─ MacroView.load_from_analyzer()
```

---

## 🎨 UI状態遷移

```
┌──────────────┐
│ Welcome      │
│ Screen       │
└──────┬───────┘
       │ 「➕ 新規プロジェクト」
       ↓
┌──────────────┐
│ Project      │
│ Dialog       │
└──────┬───────┘
       │ 「🚀 分析開始」
       ↓
┌──────────────┐
│ Loading      │
│ Screen       │
└──────┬───────┘
       │ 完了
       ↓
┌──────────────┐
│ MacroView    │
│ (全体マップ)  │
└──────┬───────┘
       │ 「🔍 詳細比較」
       ↓
┌──────────────┐
│ MicroView    │
│ (詳細比較)    │
└──────────────┘
```

---

## ⚙️ エンジン初期化

```python
def _initialize_engines(self):
    """エンジンを初期化"""
    try:
        # OCRエンジン
        self.ocr_engine = OCREngine(credentials_path="credentials.json")
        
        # Analyzer
        self.analyzer = ContentAnalyzer(ocr_engine=self.ocr_engine)
        
        print("✅ エンジン初期化完了")
        
    except Exception as e:
        print(f"⚠️ エンジン初期化エラー: {str(e)}")
        # OCRなしでも動作可能
        self.analyzer = ContentAnalyzer()
```

**注意:**
- `credentials.json` がない場合でも動作します
- OCRなしモードで起動されます

---

## 🐛 エラーハンドリング

### 分析エラー

```python
def _on_analysis_error(self, error):
    """分析エラー時の処理"""
    print(f"❌ 分析エラー: {str(error)}")
    traceback.print_exc()
    
    messagebox.showerror(
        "エラー",
        f"分析中にエラーが発生しました:\n\n{str(error)}"
    )
    
    # ウェルカム画面に戻る
    self._show_welcome()
```

### インポートエラー

```python
try:
    from app.gui.navigation import NavigationPanel
    # ...
except ImportError as e:
    print(f"⚠️ インポートエラー: {e}")
    # 相対インポート
    from .navigation import NavigationPanel
```

---

## 🧪 テスト

### テストスクリプト

```bash
python test_main_window.py
```

### 手動テスト手順

1. **起動確認**
   - ウェルカム画面が表示される
   - NavigationPanelが表示される

2. **新規プロジェクト**
   - 「➕ 新規プロジェクト」クリック
   - ダイアログが表示される
   - 設定を入力して「🚀 分析開始」

3. **ローディング**
   - ローディング画面が表示される
   - メッセージが更新される

4. **完了**
   - 完了ダイアログが表示される
   - MacroViewに遷移する

---

## 📈 実装統計

| 項目 | 詳細 |
|------|------|
| ファイル | `app/gui/main_window.py` |
| 行数 | ~350行 |
| クラス数 | 1 (MainWindow) |
| メソッド数 | 20+ |
| コールバック数 | 9 |

---

## 🔜 今後の実装

### 短期
- [ ] OCR実行機能の完全統合
- [ ] Excel出力機能
- [ ] プロジェクト保存/読込

### 中期
- [ ] クロール進捗のリアルタイム表示
- [ ] エラーリカバリー機能
- [ ] 詳細ログビューア

---

## 💡 使用例

### 基本的な起動

```python
from app.gui.main_window import MainWindow

app = MainWindow()
app.mainloop()
```

### カスタムコールバック

```python
app = MainWindow()

# カスタムコールバックを追加
def custom_action():
    print("Custom action!")

app.nav_panel.callbacks["custom"] = custom_action
```

---

## 🎊 統合完了

MainWindow への統合が完了しました：

✅ NavigationPanel 統合
✅ 新規プロジェクト機能
✅ バックグラウンド処理
✅ ローディング画面
✅ エラーハンドリング
✅ ContentAnalyzer 統合

**これで、単一のエントリーポイントから全ての機能にアクセスできるようになりました！**

